import { RadioBtnGroupDirective } from './radio-btn-group.directive';

describe('RadioBtnGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new RadioBtnGroupDirective();
    expect(directive).toBeTruthy();
  });
});
