import { FormItemAddonDirective } from './form-item-addon.directive';

describe('FormItemAddonDirective', () => {
  it('should create an instance', () => {
    const directive = new FormItemAddonDirective();
    expect(directive).toBeTruthy();
  });
});
