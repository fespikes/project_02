import { ScenariosDirective } from './scenarios.directive';

describe('ScenariosDirective', () => {
  it('should create an instance', () => {
    const directive = new ScenariosDirective();
    expect(directive).toBeTruthy();
  });
});
