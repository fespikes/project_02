import { FunctionalitiesDirective } from './functionalities.directive';

describe('FunctionalitiesDirective', () => {
  it('should create an instance', () => {
    const directive = new FunctionalitiesDirective();
    expect(directive).toBeTruthy();
  });
});
