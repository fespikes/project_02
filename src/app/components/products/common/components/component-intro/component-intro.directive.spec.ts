import { ComponentIntroDirective } from './component-intro.directive';

describe('ComponentIntroDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentIntroDirective();
    expect(directive).toBeTruthy();
  });
});
