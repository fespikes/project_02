import { AdvantagesDirective } from './advantages.directive';

describe('AdvantagesDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvantagesDirective();
    expect(directive).toBeTruthy();
  });
});
