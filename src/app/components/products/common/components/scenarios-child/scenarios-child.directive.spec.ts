import { ScenariosChildDirective } from './scenarios-child.directive';

describe('ScenariosChildDirective', () => {
  it('should create an instance', () => {
    const directive = new ScenariosChildDirective();
    expect(directive).toBeTruthy();
  });
});
